## Exercise 6

I implemented DFS and BFS traversal methods. Using DFS, I calculated the total sum of all node weights recursively, and also a signed sum where the sign alternates at each level. Using BFS, I did the same but iteratively, with a simple dynamic queue I implemented.